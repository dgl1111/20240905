<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.TestMapper">
	<select id="testList" parameterType="hashmap" resultType="com.example.test1.model.Test">
		SELECT 
		* FROM TEST_PRODUCT
		WHERE PRODUCT_NO = #{productNo}
	</select>
	
	<delete id="deleteList" parameterType="hashmap" resultType="com.example.test1.model.Test">
		DELETE 
		FROM TEST_PRODUCT
		WHERE PRODUCT_NO = #{productNo}
	</delete>
	
	<update id="updateList" parameterType="hashmap" resultType="com.example.test1.model.Test">
		UPDATE TEST_PRODUCT
		SET PRODUCT_PRICE = #{productPrice}
		WHERE PRODUCT_NO = #{productNo}
	</update>
	
	
</mapper>